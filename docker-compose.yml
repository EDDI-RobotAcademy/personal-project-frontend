version: "3.7"
services:
  frontend:
    image: ghcr.io/qnfthslr/health-community:latest
    container_name: docker-frontend-health-community-server
    ports:
      - "80:80"
    networks:
      - app

networks:
  app:
    driver: bridge
