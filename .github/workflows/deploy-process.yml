name: Deploy

on: 
  push:
    branches:
      - main
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello Frontend after CI
        run: |
          echo "Hello Frontend after CI!"
